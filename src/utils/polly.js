/* ⚠️ DEPRECATED — 이 파일은 더 이상 사용되지 않습니다.
 *
 * TTS 는 Amazon Polly 직접 호출에서 Web Speech API(speechSynthesis)로
 * 교체되었습니다 (hooks/useTts.jsx 참조). AWS 자격 증명도 더 이상
 * 필요 없습니다 (.env 의 VITE_AWS_* 항목 제거 가능).
 *
 * 이 파일은 안전하게 삭제해도 됩니다.
 * (sandbox 권한 제한으로 자동 삭제 불가 — 직접 삭제 부탁드립니다.)
 */
export default null;
