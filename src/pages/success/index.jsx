/* ⚠️ DEPRECATED — 이 페이지는 더 이상 사용되지 않습니다.
 *
 * 이전엔 backend /payments/register + /payments/confirm 호출을 여기서
 * 했지만, 현재는 pay 페이지에서 processing → done 흐름으로 통합했습니다.
 *
 * 이 파일 및 src/pages/success/ 폴더는 안전하게 삭제해도 됩니다.
 * (sandbox 권한 제한으로 자동 삭제 불가 — 직접 삭제 부탁드립니다.)
 */
export default function Success() {
  return null;
}
