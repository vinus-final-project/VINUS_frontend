/* ⚠️ DEPRECATED — 이 페이지는 더 이상 사용되지 않습니다.
 *
 * 이전엔 토스 failUrl 콜백에서 실패 안내를 하고 /payment 로 복귀시켰지만,
 * 현재는 successUrl 과 failUrl 을 모두 /pay 로 지정하여 pay 페이지의
 * processing → done 흐름으로 통합했습니다.
 *
 * 이 파일 및 src/pages/fail/ 폴더는 안전하게 삭제해도 됩니다.
 * (sandbox 권한 제한으로 자동 삭제 불가 — 직접 삭제 부탁드립니다.)
 */
export default function Fail() {
  return null;
}
